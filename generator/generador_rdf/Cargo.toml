[package]
name = "rdf_generator"
version = "0.1.0"
edition = "2018"

[dependencies]
wasm-bindgen = "0.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
rand = "0.8"
getrandom = { version = "0.2", features = ["js"] }  # Add the "js" feature


[lib]
crate-type = ["cdylib"]
